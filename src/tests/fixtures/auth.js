/**
 * @constant {string} uid for testing DB transactions
 */
export const uid = 'thisisatestuid123';

/**
 * default state object for testing DB transactions
 * @prop {object} auth
 * @prop {string} auth.uid
 */
export const defaultAuthState = { auth: { uid } };